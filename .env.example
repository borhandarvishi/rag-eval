# Dense embeddings (required)
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_MODEL_PROVIDER=openai
EMBEDDING_MODEL_API_KEY=sk-...
# Optional override (default 1536 for text-embedding-3-small)
# DENSE_DIM=1536

# Milvus connection (required for evaluation)
MILVUS_HOST=localhost
MILVUS_PORT=19530
# Optional auth for Zilliz / secured clusters
# MILVUS_TOKEN=
# Optional prefix for temporary eval collections
# COLLECTION_PREFIX=ecom_rag_eval
